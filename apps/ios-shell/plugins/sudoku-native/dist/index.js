// Native-only Capacitor plugin. The production web UI detects the plugin
// through window.Capacitor.Plugins.SudokuNative, so no bundled JS import is
// required by the remote prototype shell.
export {};
