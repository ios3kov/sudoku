name: bootstrap-snapshot

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/bootstrap-snapshot.yml'

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Broken multipart importer disabled; source is imported as ordinary Git objects."
