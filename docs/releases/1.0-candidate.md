# Sudoku 1.0 — release-candidate evidence

Checkpoint: 2026-09-22. The agreed feature implementation and its full automated release gate are complete. Deployment identity and final Step 70 acceptance are not yet verified. This is release work, not additional feature scope.

## Frozen application candidate

| Evidence | Result |
| --- | --- |
| Application commit | `ec557c2827b0d2b5329425016df3dfa8ddaf4f71` |
| Application tree | `b8e913dd49e735200ad60cc88b63fb9cd042c174` |
| Final product block | PR #33 (Messenger UX 3.0), merged |
| Full PR gate | CI #285 / run `35694695249`, passed on `35d1613274cec2efcd37e2edc67ef1b82e94bcf8` |
| Full post-merge gate | CI #286 / run `35695831785`, completed / success on the application commit, 2026-09-22 06:51 UTC |
| Public production edge | Run `35696475508` / job `106644347990`, passed, 2026-09-22 06:48 UTC |
| New production deployment | Not performed by this work |
| Current running production Git SHA | Not established by the available evidence |
| Physical/live Step 70 | Still explicitly deferred; not passed |

Full CI includes API/OpenMLS tests, dependency audits, UI and domain regressions, lint/typecheck/build/budget checks, browser acceptance, operations scripts, MinIO images, Compose policy and production API/Web image builds. The successful run does not mean there are no warnings or certify physical-device behavior.

The earlier pending CI statements in the PR and working documents are superseded by this exact completed-run evidence. No application or production configuration change is made by this operations branch. Continue to deploy the immutable application commit, not this branch or a moving main ref.

## Verified public edge

The immutable candidate's `scripts/smoke-production.sh` passed against `sudoku.moscow` and `assets.sudoku.moscow`:

- DNS resolution and certificate-valid HTTPS;
- required security headers and expected CSP app/asset/WebSocket origins;
- API readiness;
- asset TLS endpoint and its required security headers;
- HTTP-to-HTTPS redirects.

Only the documented ports on `185.31.167.36` were probed. TCP 80/443 were reachable; TCP 22/3000/5432/6379/8000/9000/9001 timed out from the GitHub hosted runner. There were no unexpected reachable ports within this limited set. A timeout is an observation from this source, not proof of a specific firewall rule or a comprehensive port scan.

Retained raw non-secret evidence:
- `docs/releases/evidence/2026-09-22-public-edge.txt`;
- `docs/releases/evidence/2026-09-22-public-ports.json`;
- original GitHub artifact ID `10680039523`, ZIP SHA256 `1bb6d6a4bd9d03de78da8365509dba92561083308f503972b22c20f35ca45471` (downloaded and verified).

The checker SHA identifies the verification script, not the code currently running on the production server. Historical exact-SHA live smoke for `68211e02` remains historical. The new public-edge success must not overwrite the deployment identity.

## Access boundary and next executable step

GitHub repository/actions access works. The authenticated Selectel browser check did not start because its provider required credit. The local execution environment has no SSH executable, mounted SSH identity or SSH agent; a direct SSH command therefore did not execute. No authenticated server session was established. No authentication bypass, password reset, key upload, public SSH exposure, purchase or production mutation was attempted.

Use the existing trusted administrator SSH session to collect the following non-secret inventory before deployment. This command does not change the checkout, start/stop containers or print environment values:

```bash
cd /home/deploy/sudoku &&
git rev-parse HEAD &&
git status --short &&
docker compose --env-file .env.production -f compose.yaml -f compose.production.yaml ps &&
docker compose --env-file .env.production -f compose.yaml -f compose.production.yaml images
```

Checkout identity alone is not proof of container identity; retain both outputs. Do not send private keys, `.env.production`, cookies, passwords or decrypted messages.

## Remaining release tasks and stop criteria

1. Record the current on-host checkout and running image identities. Resolve unexpected local source changes without overwriting them.
2. Preserve the rollback point and a valid database/encrypted-object backup. Existing backup tooling stops application services for maintenance; it is not non-disruptive and a created backup is not a passed restore drill.
3. Deploy the verified application commit with the canonical production preflight and Compose configuration. Run post-deploy readiness and edge smoke, and record the exact resulting release evidence. Do not infer deployment from CI or the pre-deployment public-edge check.
4. Complete the already-agreed Step 70 acceptance: invite acceptance, physical iOS/Android installed PWA behavior, real worker/realtime delivery, direct/group MLS, encrypted attachment/voice, offline/reload/revocation, persistence and controlled restore.

No new feature, redesign or automatic global-audit cycle is added. Do not mark final 1.0 production-verified until the deployed release and all Step 70 criteria have recorded evidence. Stop on failed preflight, backup, readiness or smoke. Do not automatically downgrade database schema or run destructive restore. The final checkpoint and evidence paths are also recorded in PR #33.

## Verification of this operations change

Before publication: YAML structure, read-only workflow permissions, immutable checkout ref, Bash syntax and inline Python syntax checked. After execution: successful remote workflow result, archive SHA256, expected evidence markers and limited-port result verified. The operator inventory snippet passes Bash syntax review and contains no mutating command. No full application rerun is claimed for documentation changes; the application remains the exact CI #286-verified commit.
