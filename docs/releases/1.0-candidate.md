# Sudoku 1.0 — release-candidate evidence

Checkpoint: 2026-09-22. This record is release work, not additional feature scope.

## Frozen application candidate

- Application commit: `ec557c2827b0d2b5329425016df3dfa8ddaf4f71`.
- Application tree: `b8e913dd49e735200ad60cc88b63fb9cd042c174`.
- Final product block: merged PR #33 (Messenger UX 3.0).
- Full PR gate: CI #285 / run `35694695249` passed on `35d1613274cec2efcd37e2edc67ef1b82e94bcf8`.
- Post-merge gate: CI #286 / run `35695831785` is pending at this checkpoint. Do not infer its outcome from PR CI.
- No application or production configuration change is made by this branch.

## Remaining release tasks

1. Capture completed post-merge CI on the exact application commit.
2. Run the repository public-edge smoke and the documented limited public-port check without authenticating, changing infrastructure, or reading private content.
3. Deploy the verified application SHA through the existing administrator SSH route after a valid backup, then capture on-host SHA and post-deploy smoke. Existing backup tooling enters maintenance mode; do not call it a non-disruptive operation.
4. Complete the explicitly deferred Step 70 acceptance: physical devices, invite acceptance, real worker/realtime delivery, encrypted attachment/voice, offline/reload/revocation, persistence and controlled restore.

## Evidence boundaries and stop criteria

The read-only workflow uses the immutable candidate's smoke script and contents:read. It does not use deployment credentials, change firewall rules, deploy, reboot, restore, or write application data. It probes only the nine documented service ports on the user's existing public host. Non-reachability from a hosted runner is not proof of a particular firewall rule. A successful public-edge check does not identify the deployed Git SHA.

The authenticated Selectel browser access check did not start because the connected browser provider required credit. The local execution environment has no mounted SSH identity or SSH agent. No authentication bypass, password reset, key upload, or public SSH exposure is authorized by this verification task.

Do not mark production verified until the exact running release and all Step 70 criteria have recorded evidence. Do not bypass failed CI, preflight, backup, readiness or smoke. Do not automatically downgrade database schema or run destructive restore. Inspect failures and preserve the previous verified release and non-secret operation evidence.

Historical live smoke for `68211e02` remains historical; it is not the current deployment identity.
